import config from 'react-native-config';

let primary = config.PRIMARY_COLOR
let black = config.BLACK_COLOR
let dividerColor = config.DIVIDER_COLOR
let red = config.RED_COLOR
let frameBackground = config.FRAME_BACKGROUND
let white = config.WHITE_COLOR
let gray = config.GRAY_COLOR
let blue = config.BLUE_COLOR

export default {
  primary,
  black,
  dividerColor,
  red,
  frameBackground,
  white,
  gray,
  blue
};
